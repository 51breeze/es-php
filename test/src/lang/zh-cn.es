
import config, {connections, top} from '../config.es'
import * as config2 from '../config.es'

let data = @Readfile('com')

export const home={
    data,
    top,
    config,
    config2,
    connections,
    name:'ssss9-9999'
}